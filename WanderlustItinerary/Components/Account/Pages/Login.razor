@page "/Account/Login"

@using System.ComponentModel.DataAnnotations
@using Microsoft.AspNetCore.Authentication
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Identity
@using WanderlustItinerary.Data

@inject SignInManager<IdentityUser> SignInManager
@inject ILogger<Login> Logger
@inject NavigationManager NavigationManager
@inject IdentityRedirectManager RedirectManager

@attribute [AllowAnonymous]

<PageTitle>Login</PageTitle>

<style>
    /* same “travel-pattern” effect from your HTML */
    .travel-pattern {
        background-color: #fff;
        background-image: url(https://lh3.googleusercontent.com/aida-public/AB6AXuDxTJfrKlDa5p6gS3Tr1hVJpZchXsRbal_7wKKKa1u2ShxWwqKnNePoL9Mvcq47rgv0TvdVZPLrdTKMdEQida8FIpz34dw2JOh4Ir-3wJuavNn-6wr1AtXgsBDgSP8n4qWkVBxC0M-xJux9662P7JpCNScErkxT1wP__06HvG8UW5l1WkNDbzjgvjbn64q4u1I6DKXR0gzkouFibcoDH8qsgOU4Uw_WEORA_CzsRXlY1D46dkzAdYk9YyXiG7sSFt-I_tuwuG9u1gw);
    }

    .dark .travel-pattern {
        background-color: #0f172a;
        background-image: url(https://lh3.googleusercontent.com/aida-public/AB6AXuA0EuMaL_tm9s2ayLAh5HyqhubfPEmsDQbex4-2unbTGO3HqURrbOE9CBcyaJQ2lco92X8NBnHdQ-WtEJeYuhRTcNvdM6TIUQn25QCp3Xxs4KkEGZzJPm9IJ1PG6KjwqFJpAT0FRRt2_5FA9LAqfoMCpoq4b7idK4urbWZYapu8zVbZThDEN47tohqEvq8xdS2Zb60BUqlijZ6K0S3BzGLML5sTX2jxbVAD0FmadJ7Y4BinlN9v0u-H-tFO4TeNWUE611Vx--UNma8);
    }
</style>

<div class="bg-background-light dark:bg-background-dark font-body antialiased transition-colors duration-300 min-h-screen flex flex-col travel-pattern">

    <nav class="w-full py-4 px-6 md:px-12 flex justify-between items-center bg-surface-light/80 dark:bg-surface-dark/80 backdrop-blur-md border-b border-gray-200 dark:border-gray-700 sticky top-0 z-50">
        <div class="flex items-center space-x-2">
            <span class="material-icons text-primary text-3xl">flight_takeoff</span>
            <a href="/" class="font-display font-bold text-2xl text-gray-800 dark:text-white tracking-tight">Wanderlust</a>
        </div>

        <div class="flex items-center space-x-4">
            <!-- Dark mode toggle (works without JS) -->
            <button class="p-2 rounded-full hover:bg-gray-100 dark:hover:bg-gray-700 text-gray-600 dark:text-gray-300 transition-colors"
                    type="button" @onclick="ToggleDark">
                <span class="material-icons @(IsDark ? "hidden" : "")">dark_mode</span>
                <span class="material-icons @(!IsDark ? "hidden" : "")">light_mode</span>
            </button>
        </div>
    </nav>

    <main class="flex-grow flex items-center justify-center p-6 relative overflow-hidden">
        <div class="absolute top-20 left-10 w-64 h-64 bg-primary/10 rounded-full blur-3xl -z-10 animate-pulse"></div>
        <div class="absolute bottom-20 right-10 w-96 h-96 bg-blue-500/10 rounded-full blur-3xl -z-10 animate-pulse delay-700"></div>

        <div class="w-full max-w-5xl bg-surface-light dark:bg-surface-dark rounded-2xl shadow-2xl overflow-hidden flex flex-col md:flex-row min-h-[600px] border border-gray-100 dark:border-gray-700 transition-colors duration-300">

            <!-- LEFT: Form -->
            <div class="w-full md:w-1/2 p-8 md:p-12 flex flex-col justify-center">
                <div class="mb-8">
                    <h1 class="font-display text-4xl font-bold text-gray-900 dark:text-white mb-2">Welcome Back</h1>
                    <p class="text-gray-500 dark:text-gray-400">Please enter your details to sign in.</p>
                </div>

                <StatusMessage Message="@errorMessage" />

                <EditForm Model="Input" method="post" OnValidSubmit="LoginUser" FormName="login" class="space-y-6">
                    <DataAnnotationsValidator />
                    <ValidationSummary class="text-red-500 text-sm" role="alert" />

                    <div>
                        <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" for="email">Email Address</label>
                        <div class="relative">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <span class="material-icons text-gray-400 text-xl">email</span>
                            </div>

                            <InputText id="email"
                                       @bind-Value="Input.Email"
                                       autocomplete="username"
                                       aria-required="true"
                                       class="block w-full pl-10 pr-3 py-3 border border-gray-300 dark:border-gray-600 rounded-lg bg-gray-50 dark:bg-gray-800 text-gray-900 dark:text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-shadow sm:text-sm"
                                       placeholder="you@example.com" />
                        </div>
                        <ValidationMessage For="() => Input.Email" class="text-red-500 text-sm mt-1" />
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" for="password">Password</label>
                        <div class="relative">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <span class="material-icons text-gray-400 text-xl">lock</span>
                            </div>

                            <InputText id="password"
                                       type="password"
                                       @bind-Value="Input.Password"
                                       autocomplete="current-password"
                                       aria-required="true"
                                       class="block w-full pl-10 pr-3 py-3 border border-gray-300 dark:border-gray-600 rounded-lg bg-gray-50 dark:bg-gray-800 text-gray-900 dark:text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-shadow sm:text-sm"
                                       placeholder="••••••••" />
                        </div>
                        <ValidationMessage For="() => Input.Password" class="text-red-500 text-sm mt-1" />
                    </div>

                    <div class="flex items-center justify-between">
                        <div class="flex items-center">
                            <InputCheckbox id="remember-me"
                                           @bind-Value="Input.RememberMe"
                                           class="h-4 w-4 text-primary focus:ring-primary border-gray-300 rounded bg-gray-50 dark:bg-gray-700 dark:border-gray-600" />
                            <label class="ml-2 block text-sm text-gray-600 dark:text-gray-400" for="remember-me">
                                Remember me
                            </label>
                        </div>

                        <div class="text-sm">
                            <a class="font-medium text-primary hover:text-blue-600 transition-colors"
                               href="/Account/ForgotPassword">
                                Forgot password?
                            </a>
                        </div>
                    </div>

                    <div>
                        <button class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-sm font-bold text-white bg-primary hover:bg-sky-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-all transform hover:-translate-y-0.5"
                                type="submit">
                            Sign in
                        </button>
                    </div>
                </EditForm>

                <div class="mt-8 pt-6 border-t border-gray-100 dark:border-gray-700 text-center">
                    <p class="text-sm text-gray-600 dark:text-gray-400">
                        Don't have an account yet?
                        <a class="font-bold text-primary hover:text-blue-600 transition-colors"
                           href="@(NavigationManager.GetUriWithQueryParameters("Account/Register", new Dictionary<string, object?> { ["ReturnUrl"] = ReturnUrl }))">
                            Register now
                        </a>
                    </p>
                </div>
            </div>

            <!-- RIGHT: Image panel -->
            <div class="hidden md:block w-1/2 relative bg-gray-900">
                <img alt="Beautiful landscape of Santorini Greece with blue domes"
                     class="absolute inset-0 h-full w-full object-cover opacity-80 mix-blend-overlay"
                     src="https://lh3.googleusercontent.com/aida-public/AB6AXuB_FrZWPQfaEhpE_vM9YBx5hDec3WdVaG9t48oEtrToYo7rQInUIhtNkMQh6Hk-ImD1eb5qCgTaI43_wIR-KKLhU67MoJLmrIVXW6I7_vAQFZWF8rwRkAHpZDxsWN1XVMi68hkFcDG4S7-jXK5dF3Nn5jJ0A6BfZ7CMC99D6lVHfFzlq1ZxCrWU7Cnyw47b5BlulEUltfqlgvsRKbUu1KBFEPII6i_e9XURCYQOy2N1Gk-0S1Q_2R0F3a1F6PSuR3_THw5KT59MGaw" />
                <div class="absolute inset-0 bg-gradient-to-br from-primary/80 to-purple-900/80 mix-blend-multiply"></div>

                <div class="relative z-10 h-full flex flex-col justify-between p-12 text-white">
                    <div class="flex justify-end">
                        <span class="material-icons text-white/50 text-6xl">public</span>
                    </div>

                    <div>
                        <blockquote class="font-display text-3xl font-medium leading-tight mb-6">
                            "The world is a book and those who do not travel read only one page."
                        </blockquote>
                        <cite class="block text-white/80 not-italic font-body">– St. Augustine</cite>

                        <div class="mt-8 flex space-x-2">
                            <div class="w-12 h-1 bg-white rounded-full"></div>
                            <div class="w-2 h-1 bg-white/30 rounded-full"></div>
                            <div class="w-2 h-1 bg-white/30 rounded-full"></div>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </main>

    <footer class="w-full py-6 text-center text-sm text-gray-500 dark:text-gray-400 bg-surface-light dark:bg-surface-dark border-t border-gray-200 dark:border-gray-800">
        <p>© 2026 Wanderlust Itinerary System. All rights reserved.</p>
    </footer>
</div>

@code {
    private string? errorMessage;

    [CascadingParameter]
    private HttpContext HttpContext { get; set; } = default!;

    [SupplyParameterFromForm]
    private InputModel Input { get; set; } = new();

    [SupplyParameterFromQuery]
    private string? ReturnUrl { get; set; }

    // just UI toggle (no JS required for this file)
    private bool IsDark;

    protected override async Task OnInitializedAsync()
    {
        // Clear external cookie on GET, same pattern as Register
        if (HttpMethods.IsGet(HttpContext.Request.Method))
        {
            await HttpContext.SignOutAsync(IdentityConstants.ExternalScheme);
        }
    }

    private void ToggleDark() => IsDark = !IsDark;

    private async Task LoginUser()
    {
        var result = await SignInManager.PasswordSignInAsync(
            Input.Email,
            Input.Password,
            Input.RememberMe,
            lockoutOnFailure: false);

        if (result.Succeeded)
        {
            Logger.LogInformation("User logged in.");
            RedirectManager.RedirectTo(ReturnUrl);
            return;
        }

        errorMessage = "Invalid login attempt.";
    }

    private sealed class InputModel
    {
        [Required, EmailAddress]
        public string Email { get; set; } = "";

        [Required, DataType(DataType.Password)]
        public string Password { get; set; } = "";

        public bool RememberMe { get; set; }
    }
}
